name: gemstone_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  firebase_database: ^12.1.4
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Icon banvnya sathi ha package ithe add kela ahe
  flutter_launcher_icons: "^0.13.1"

# Icon sathi setting ithe dili ahe
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/diamond.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true

  assets:
    - assets/